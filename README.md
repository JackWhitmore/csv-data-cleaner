# csv-data-cleaner
A command-line utility that cleans messy CSV files by removing duplicate rows, correcting inconsistent formatting, validating data types, and exporting structured datasets. Designed for analysts and developers working with large collections of tabular data.
